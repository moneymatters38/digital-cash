import hashlib

def get_proof(header, nonce):
    preimage = 
    proof_hex = hashlib.sha256(preimage).hexdigest()

if __name__ == "__main__":
    header = "hello"
    for nonce in range(10):
        print(get_proof(header, nonce))
